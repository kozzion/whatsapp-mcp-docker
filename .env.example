# Absolute host directory where the WhatsApp session + message database +
# downloaded media are stored (bind-mounted into the container). Use forward
# slashes on Windows. Copy this file to `.env` and adjust the path.
PATH_DIR_DATA_WHATSAPPMCP=C:/data/kozzion/whatsapp-mcp
