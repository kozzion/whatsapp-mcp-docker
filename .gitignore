*.db
claude_desktop_config.json
whatsapp.log
# Python / runtime artifacts
__pycache__/
*.pyc
.venv/
# WhatsApp session + message store (created at runtime / lives in the Docker volume)
store/
**/store/
